apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: atmpl

resources:
  - ../../base
  - namespace.yaml

patches:
  - path: deployment.yaml
  - path: configmap.yaml
  - path: hpa.yaml

images:
  - name: ghcr.io/mohamed3042/enterprise-ai-automation-templates
    newTag: v0.3.0
